import { StyleSheet } from 'react-native';

export const Globals = StyleSheet.create({
	input: {
		width: '100%',
		// height: 100,
		padding: 1,
		borderRadius: 6,
		borderColor: '#0f172b',
		borderWidth: 2,
	},
	header: {},
	text: {},
});
